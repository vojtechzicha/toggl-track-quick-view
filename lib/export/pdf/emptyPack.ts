// The template pack used when none is configured (a plain clone). See ./pack.ts.

import type { TemplatePack } from './types';

const pack: TemplatePack = {
  name: 'none',
  templates: [],
};

export default pack;
